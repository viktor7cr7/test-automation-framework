import dotenv from 'dotenv';
dotenv.config();

export const USER_EMAIL_REGISTER = `${process.env.USER_EMAIL_REGISTER}`;

export const USER_EMAIL_LOGIN = `${process.env.USER_LOGIN}`;
export const USER_PASSWORD = `${process.env.USER_PASSWORD}`;
export const USER_NAME = `${process.env.USER_NAME}`;

export const DOLLAR_EXCHANGE_RATE = 87.9;
