Вызываем openCart на странице page = class DashboardPage extends _basePage.BasePage {
  constructor(...args) {
    super(...args);
    this.cart = (_initProto(this), this.page.getByTestId('cart'));
    this.uniqeElement = this.cart;
  }
  async openCart() {
    await this.cart.click();
  }
}
